/**
 * Opens browser in maximized mode.
 */
export const START_MAXIMIZED = "--start-maximized";

/**
 * Opens browser on private mode.
 */
export const INCOGNITO = "--incognito";

/**
 * Runs browser using CLI.
 */
export const HEADLESS = "--headless";

/**
 * Bypasses OS security model.
 */
export const NO_SANDBOX = "--no-sandbox";

/**
 * Disables GPU computation (applicable to Windows OS only).
 */
export const DISABLE_GPU = "--disable-gpu";

/**
 * Ignores certificate errors.
 */
export const IGNORE_CERTIFICATE_ERRORS = "--ignore-certificate-errors";

/**
 * Disables to check if Google Chrome is default browser on your device.
 */
export const NO_DEFAULT_BROWSER_CHECK = "--no-default-browser-check";

/**
 * Disables popup blocking.
 */
export const DISABLE_POPUP_BLOCKING = "--disable-popup-blocking";

/**
 * Disables installed extensions(plugins) of Google Chrome.
 */
export const DISABLE_EXTENSIONS = "--disable-extensions";

/**
 * Disables default web apps on Google Chrome’s new tab page
 * <p>
 * Chrome Web Store, Google Drive, Gmail, YouTube, Google Search, etc.
 */
export const DISABLE_DEFAULT_APPS = "--disable-default-apps";

/**
 * Disables Google translate feature.
 */
export const DISABLE_TRANSLATE = "--disable-translate";

/**
 * Disables detection for client side phishing.
 */
export const DISABLE_CLIENT_SIDE_PHISHING_DETECTION = "--disable-client-side-phishing-detection";

/**
 * Overcomes limited resource problems.
 */
export const DISABLE_DEV_SHM_USAGE = "--disable-dev-shm-usage";
